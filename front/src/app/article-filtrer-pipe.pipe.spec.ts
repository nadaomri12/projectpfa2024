import { ArticleFiltrerPipePipe } from './article-filtrer-pipe.pipe';

describe('ArticleFiltrerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleFiltrerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
